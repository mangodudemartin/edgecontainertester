apiVersion: apps/v1
kind: Deployment
metadata:
  name: edgecontainertester
  namespace: default
  labels:
    app: edgecontainertester
spec:
  replicas: 1
  selector:
    matchLabels:
      app: edgecontainertester
  template:
    metadata:
      labels:
        app: edgecontainertester
    spec:
      containers:
        - name: edgecontainertester
          # Replace REGISTRY_PLACEHOLDER with your ACR login server before enabling Flux,
          # or let the CI workflow populate this on the first push to main.
          image: csdevedge.azurecr.io/edgecontainertester:bc57e5b7437c4a4b75a495368e80e1114c30c019
          ports:
            - containerPort: 8080
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "200m"
              memory: "128Mi"
