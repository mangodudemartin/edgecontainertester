apiVersion: v1
kind: Service
metadata:
  name: edgecontainertester
  namespace: default
  labels:
    app: edgecontainertester
spec:
  type: ClusterIP
  selector:
    app: edgecontainertester
  ports:
    - name: http
      port: 80
      targetPort: 8080
